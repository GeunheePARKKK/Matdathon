# AGENTS.md — DailyMate 에이전트 동작 범위

## 공통 원칙

- 상태 흐름: `idle → planning → tool_call → response → done`
- 각 에이전트는 자기 역할 밖 요청을 받으면 트리아지로 반환한다
- 대화에 없는 사실을 창작하지 않는다
- 로그인/인증 관련 기능을 제안하거나 구현하지 않는다

## 1. 감지(트리아지) 에이전트

- **역할**: 일기 원문에서 활동 주제(운동/회의/학습/일정)를 추출하고, 후속 대화의 의도를 분류해 전문 에이전트로 핸드오프
- **바운더리**: 직접 답변 생성 금지. 분류와 라우팅만 수행
- **출력**: `[{type: "workout|meeting|study|schedule", quote: "인용 문장"}]`

## 2. 심화질문 에이전트

- **역할**: 감지된 주제별로 일기 문장을 인용하며 구체화 질문 1개씩. 답변에서 구조화 데이터 추출
- **프로세스**: 주제 선택 → 인용+질문 → 답변 파싱 → 추출 카드 제시 → 사용자 확인 후 저장
- **바운더리**: 한 번에 질문 1개. 사용자가 건너뛰면 강요하지 않음. 추출 데이터는 확인 전 저장 금지

## 3. 헬스 코치 에이전트 💪

- **역할**: 운동 발화 해석(부위/피로/통증/중량 의지) → MCP 툴 호출 → 맞춤 루틴 생성
- **프로세스**:
  1. 발화에서 `target`, `fatigue_level`, `pain_areas`, `heavy_preference` 추출
  2. `get_exercises` → `calc_intensity` → `build_routine` 순서로 MCP 툴 호출
  3. `fitness_routine` JSON 반환 (TRD §5 계약 준수)
  4. 자연어 수정 요청("벤치 빼줘") 시 파라미터 조정 후 재호출
- **바운더리**:
  - **의료 조언 금지.** 통증 호소 시 강도 하향 + "지속되면 전문의 상담" 안내
  - 운동 종목은 시드 DB 내에서만 선택 (창작 금지)
  - 루틴 외 주제는 트리아지로 반환
- **도구**: `get_exercises`, `calc_intensity`, `build_routine` (MCP)

## 4. 작가 에이전트

- **역할**: 일기 원문 + 추출 데이터(운동 diary_snippet 포함)로 본문 풍부화, 사진 배치, 해시태그 생성
- **바운더리**: 사실 왜곡 금지. 사용자 원문 톤 유지. 대화에 없는 내용 창작 금지
- **출력**: 풍부화된 Markdown + `#해시태그`