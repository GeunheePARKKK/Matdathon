# 📋 PRD — DailyMate (개인 생산성 향상 에이전트 앱)

> Product Requirements Document
> 버전: 1.0 (최종) · 작성일: 2026-08-22

---

## 1. 제품 개요

### 1.1 한 줄 정의
사용자가 직접 쓴 일기를 에이전트가 읽고 문맥을 파악해 심화 질문을 던지고, 답변으로 일기를 풍부하게 완성하며, 운동 발화에서는 **컨디션 맞춤 운동 루틴까지 생성**하고, 일정·회의록·운동기록을 외부 앱에 저장하는 **개인 생산성 향상 에이전트 웹앱**.

### 1.2 문제 정의 (타겟 사용자의 실제 문제)
- 일기를 쓰면 "오늘 뭐 했다" 수준에서 끝나 디테일이 남지 않는다.
- 하루 동안 발생한 정보(지출, 회의 내용, 운동 기록, 내일 일정)가 여러 앱에 흩어지거나 기록되지 않는다.
- 회고(일기)와 계획(일정)이 분리되어 생산성 루프가 끊긴다.
- 운동 계획은 **전날의 피로·통증 같은 컨디션**을 반영해야 하지만, 일기(컨디션 기록)와 운동 앱(계획)이 단절되어 매번 수동으로 판단해야 한다.

### 1.3 해결 방식
1. **회고 심화**: 에이전트가 일기 문맥을 감지하고 구체적 질문으로 디테일을 끌어낸다.
2. **자동 구조화**: 답변에서 운동/회의/학습/지출/일정 데이터를 자동 추출한다.
3. **루프 연결**: "내일 뭐 할 거예요?" 질문 → 일정 등록 → 다음 날 리마인드 → 저녁 회고로 이어지는 **계획→실행→회고 사이클**을 만든다.
4. **컨디션 맞춤 코칭**: 일기·대화에서 드러난 컨디션(통증/피로/장비·볼륨 선호)을 헬스 코치 에이전트가 해석하고, MCP 툴로 **결정적(deterministic) 맞춤 루틴**을 생성한다.
5. **외부 연동**: MCP로 Notion·Google Calendar에 저장한다 (토큰 없으면 Mock 모드).

### 1.4 타겟 사용자
- 일기·기록 습관을 만들고 싶지만 꾸준히 못 하는 사람
- 회의록, 운동 기록, 지출을 따로따로 관리하는 데 지친 사람
- 컨디션에 맞는 운동 계획을 매번 고민하는 헬스 초·중급자

### 1.5 생산성 향상 효과 (데모 기준 입증 포인트)
| 기존 워크플로 | DailyMate |
|---|---|
| 일기 앱 + 운동 앱 + 캘린더 + 노션을 오가며 15분+ 기록 | 일기 한 번 작성 → 질문 답변 → 자동 구조화·저장 (5분 내) |
| "어제 어깨 했으니 오늘 뭐 하지?" 매번 검색·고민 | 대화 이력의 컨디션을 기억해 루틴 자동 생성 (수 초) |
| 내일 계획이 머릿속에만 존재 | 대화 종료 시 일정 추출 → 확정/미정 구분 → 캘린더 등록 |

---

## 2. 제약 조건 (해커톤 필수 요건)

| # | 요건 | 구현 상태 |
|---|---|---|
| R1 | 웹 앱으로 개발 | ✅ React + Vite (모바일 우선 반응형) |
| R2 | GitHub Copilot SDK 사용 | ✅ `GitHub.Copilot.SDK` — CopilotClient로 모델 연결 |
| R3 | Microsoft Agent Framework 사용 | ✅ `Microsoft.Agents.AI.GitHub.Copilot` — AIAgent 4종 |
| R4 | Azure 클라우드 배포 | ✅ azd + Aspire → Azure Container Apps |
| R5 | **로그인/인증 기능 구현 금지** | ✅ 무로그인, 로컬 세션 기반 |
| R6 | 분산 클라우드 환경(트래픽) 고려 | ✅ Aspire 서비스 분리·헬스체크·OTel |

---

## 3. 기능 요구사항

### F1. 홈 대시보드
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F1-1 | 날짜별 인사말과 "오늘 일기 쓰기" CTA를 표시한다 | P0 |
| F1-2 | 어제 등록한 "오늘 하기로 했던 일"을 체크박스 목록으로 표시하고 완료 토글할 수 있다 | P0 |
| F1-3 | 주간 통계(일기 작성일 수 / 회의록 수 / 등록 일정 수)를 표시한다 | P1 |
| F1-4 | 최근 일기 목록(날짜·제목·사진 수)을 표시하고 상세로 이동할 수 있다 | P0 |

### F2. 일기 직접 작성
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F2-1 | 자유 텍스트로 일기를 작성할 수 있다 (자동 저장: localStorage, 2초 디바운스) | P0 |
| F2-2 | 작성 중 에이전트가 활동(운동/회의/공부/지출/기타)을 감지해 해당 구간을 색상 하이라이트한다 | P0 |
| F2-3 | 사진을 다중 첨부하고 미리보기할 수 있다 | P1 |
| F2-4 | "다 썼어요" 버튼으로 에이전트 대화 단계로 이동한다 | P0 |

### F3. 에이전트 심화 질문
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F3-1 | 감지된 주제별로 원문 인용과 함께 심화 질문을 한다 (주제당 최대 2개) | P0 |
| F3-2 | 답변에서 구조화 데이터(운동/회의록/학습/지출)를 추출한다 | P0 |
| F3-3 | 대화 중 사진 첨부 시 관련 주제에 자동 연결한다 | P1 |
| F3-4 | 마지막에 반드시 "내일은 뭘 할 예정이에요?"를 질문한다 | P0 |
| F3-5 | 내일 계획 답변에서 일정을 추출하고 **확정/미정**을 구분한다 ("~할까 생각중" → 미정) | P0 |
| F3-6 | 일정 미리보기 카드에서 체크박스로 선택 후 등록할 수 있다 (사용자 승인 필수) | P0 |
| F3-7 | 사용자가 답변을 건너뛰면 재촉 없이 다음 주제로 넘어간다 | P1 |

### F4. 일기 완성 (풍부화)
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F4-1 | 원문 흐름을 유지하며 답변 디테일을 해당 문단에 자연스럽게 삽입한다 (삽입부 굵게 표시) | P0 |
| F4-2 | 사진을 관련 문단 아래 자동 배치하고 캡션을 단다 | P1 |
| F4-3 | 해시태그를 자동 생성한다 | P2 |
| F4-4 | "다시 꾸미기" / "직접 수정"이 가능하다 | P1 |
| F4-5 | 함께 저장되는 구조화 데이터 요약을 표시한다 | P0 |

### F5. 외부 연동 (MCP)
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F5-1 | Notion MCP로 일기 본문 + 메타데이터를 저장할 수 있다 | P0 |
| F5-2 | Google Calendar MCP로 등록 일정을 동기화할 수 있다 | P1 |
| F5-3 | Markdown / JSON / PDF로 내보내기할 수 있다 | P2 |
| F5-4 | MCP 미연결 시 목(mock) 모드로 전체 플로우가 동작한다 (UI에 목 모드 표기) | P0 |

### F6. 헬스 코치 (컨디션 맞춤 루틴) 💪
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F6-1 | 대화 중 운동 루틴 요청을 감지하면 헬스 코치 에이전트로 핸드오프한다 (인터뷰 흐름은 유지) | P0 |
| F6-2 | 발화·대화 이력에서 부위(target)/피로/통증/중량·장비·볼륨 선호를 추출한다 | P0 |
| F6-3 | **부위가 확인되지 않으면 루틴을 생성하지 않고 되묻는다** ("오늘 어느 부위 운동하실 예정이에요?") | P0 |
| F6-4 | 이전 턴에서 언급된 컨디션(결림/통증/선호)은 같은 세션의 후속 루틴 요청에 반드시 누적 반영한다 | P0 |
| F6-5 | MCP 툴 체인(`get_exercises → calc_intensity → build_routine`)으로 **결정적** 루틴을 생성한다 | P0 |
| F6-6 | 통증(pain) 부위 부하 운동은 **전면 제외**, 결림/피로(fatigue)는 RPE·세트 하향 + 머신 대체로 유지한다 | P0 |
| F6-7 | 루틴은 워밍업 자동 포함, 같은 동작 패턴 최대 2개, 예상 소요시간(분)을 계산한다 | P1 |
| F6-8 | "1시간은 해야"(long) / "짧게"(short) 볼륨 요청과 "머신 위주" 장비 선호를 반영한다 | P1 |
| F6-9 | 루틴은 **루틴 카드 UI**로 렌더하고, 제외/대체/하향 사유를 notes로 투명하게 표시한다 | P0 |
| F6-10 | **의료 조언 금지** — 통증 호소 시 강도 하향 + "지속되면 전문의 상담" 안내만 제공한다 | P0 |

---

## 4. 비기능 요구사항

| ID | 요구사항 |
|---|---|
| N1 | 모바일 우선 반응형 (기준 360px) |
| N2 | 에이전트 응답은 스트리밍(SSE)으로 표시해 대기 체감 최소화 |
| N3 | 활동 감지 API는 입력 후 1.5초 디바운스로 호출 |
| N4 | LLM/MCP 장애 시에도 앱 핵심 기능은 정상 동작 — Copilot CLI 미탐지 시 Mock 모드 자동 전환 (graceful degradation) |
| N5 | Aspire를 통한 헬스체크·OpenTelemetry·서비스 디스커버리 적용 (URL 하드코딩 금지) |
| N6 | 토큰·시크릿은 환경 변수로만 주입 (코드/저장소 하드코딩 금지) |

---

## 5. 개발 단계 및 인수조건 (완료 기준 실측)

| Phase | 내용 | 인수조건 | 상태 |
|---|---|---|---|
| 1 | Aspire + web/api/agent/mcp-tool 스캐폴딩 | 대시보드에서 전 서비스 healthy + 채팅 왕복 1회 | ✅ |
| 2 | 일기 에디터 + 활동 감지 | "헬스장에서 운동했다" 입력 시 하이라이트 | ✅ |
| 3 | 인터뷰 에이전트 + 일정 등록 | "헬스장 갈까 생각중" → 미정 분류 | ✅ |
| 4 | 헬스 코치 + MCP 운동 툴 | "어깨 결려+머신 위주" → 부위 질문 → 어깨 루틴(결림 반영·OHP 제외) | ✅ |
| 5 | 작가 에이전트 + 완성 화면 + MCP 연동 | 원문에 없던 디테일이 문단에 굵게 삽입, Notion 저장 | ✅ |
| 6 | Azure 배포 + 문서 정리 | 공개 URL에서 전체 플로우 정상 동작 | 배포 진행 |

---

## 6. 범위 제외 (Out of Scope)

- 로그인/회원가입/인증 (해커톤 금지 요건)
- 다중 사용자 지원, 소셜/공유 기능
- 푸시 알림, 모바일 네이티브 앱
- 운동 DB 확장 (부위 5개 × 5~11종이면 데모 충분)
- 음성 입력 (추후 확장 후보)

---

## 7. 성공 지표 (데모 기준)

- 일기 작성 → 질문 → 완성 → Notion 저장까지 **5분 내 시연 가능**
- 에이전트 질문이 일기 문맥과 무관한 경우 0건
- 미정 일정 분류 정확도 시연 시나리오 기준 100%
- 헬스 코치 4단계 시나리오(부위 되묻기 → 결림 반영 루틴 → 1시간 확장(≥50분) → 통증 전면 제외) **웹 UI 실측 전부 통과**

---

## 8. 책임 있는 AI · 신뢰

- **AI 생성물 표시**: 에이전트 응답은 별도 말풍선/카드로 구분, MCP 목 모드는 "(목 모드)" 표기
- **사용자 통제권**: 일정 등록·외부 저장은 반드시 사용자 승인 후 실행, 부위 미확정 시 임의 추정 대신 되묻기
- **의료 안전**: 의료 조언 금지, 통증 시 강도 하향 + 전문의 안내 문구
- **환각 완화**: 루틴 계산·일정 파싱 등 결정적 작업은 MCP 툴/코드가 수행하고 LLM은 해석·표현만 담당
- **개인정보**: 무로그인 로컬 세션, 외부 전송은 사용자가 명시적으로 연동한 MCP에 한정

